# 實作計畫：台海軍事動態地圖標註與位置分析

本計畫旨在為「台海動態羅盤」新增**位置與航線分析功能**，並在「分析 (Analyze)」面板中加入一張**互動式戰術示意地圖（SVG）**，用以標註共軍各區域活動的熱度與統計數據。

---

## 使用者審查需求

> [!IMPORTANT]
> **1. 地圖呈現方式**
> 由於國防部公開資料均為文字描述（如「逾越中線進入北部空域」、「西南空域」），沒有精確的經緯度點位。因此，本計畫將設計一張**軍用戰術雷達風格的 SVG 示意地圖**，而非使用 OpenStreetMap。這能更準確地呈現「防空識別區 (ADIZ)」、「海峽中線」與各空海域區塊，且完全支援離線使用與深/淺色主題切換。
>
> **2. 互動設計**
> 點擊地圖上的各個活動區域（如西南空域、北部空域、海峽中線、周邊海域），可於側邊即時顯示該區域的詳細統計（如各機種架次、偵獲比例），並且點擊區域時可連動跳轉至「分類」頁面過濾該區紀錄。

---

## 預計變更

### 前端頁面

#### [MODIFY] [strait-watch-compass.html](file:///c:/Users/tu-hs/OneDrive/%E6%96%87%E4%BB%B6/2022_0308_MASA/2022-0708/Projects_antigravity/PRC%E5%85%B1%E8%BB%8D%E5%8B%95%E6%85%8B%E5%B9%B2%E6%93%BE/strait-watch-compass.html)

1. **UI 版面調整 (Analyze Section)**:
   * 在「分析」頁面中，於時間趨勢圖下方或右側，加入 **「戰術情勢地圖 (Tactical Situation Map)」** 區塊。
   * 版面採左右分欄或自適應網格：左側為 SVG 戰術地圖，右側為位置分析統計面板（顯示熱門區域排行、各區域活動比例）。
2. **SVG 戰術地圖繪製**:
   * 繪製中國大陸東南沿海海岸線。
   * 繪製台灣本島輪廓（使用精簡且具辨識度的 SVG Path）。
   * 繪製金門、馬祖、澎湖等離島標記。
   * 標示 **「防空識別區 (ADIZ)」** 邊界線。
   * 標示 **「海峽中線 (Median Line)」** 虛線。
   * 定義多個可點擊與高亮的互動區域：
     * 北部空域 (North Airspace)
     * 西南空域 (Southwest Airspace / SW ADIZ)
     * 東南空域 (Southeast Airspace)
     * 東部空域 (Eastern Airspace)
     * 台灣海峽/周邊海域 (Taiwan Strait / Surrounding Waters)
     * 離島區域（金門/馬祖）
3. **JS 數據解析與分類邏輯**:
   * 新增 `getLocationZone(record)` 函數，以正則表達式篩選 `location` 欄位文字：
     * `西南` / `SW` -> 西南空域
     * `北部` / `North` -> 北部空域
     * `東南` / `SE` -> 東南空域
     * `東部` / `East` / `花蓮` / `台東` -> 東部空域
     * `中線` / `逾越中線` -> 海峽中線
     * `金門` / `馬祖` / `澎湖` -> 離島區域
     * 若為 `ship` / `vessel` / `boat` 或包含 `海域` -> 周邊海域
   * 統計篩選時間內各區域的事件筆數與累計架次/艘次。
4. **地圖與統計連動邏輯**:
   * 滑鼠懸停（Hover）在 SVG 區域時，地圖區塊發光/高亮，並顯示 Tooltip 提示該區的架次/艘次總數。
   * 點擊（Click）區域時，側邊統計面板會列出該區前幾大活動機型/類型，並提供「前往明細」按鈕，點擊後會自動將「分類」頁面的過濾器設為該區域，並跳轉至「分類」頁面。

---

## 驗證計畫

### 手動驗證步驟
1. **地圖主題適應**: 切換網頁為深色（Dark）主題，確認 SVG 戰術地圖的線條、文字及熱度高亮顏色能正常自我調整，保持雷達螢幕的科技美感。
2. **範例資料測試**:
   * 點擊載入「現代範例資料」，進入「分析」頁，確認地圖上的「西南空域」、「金門海域」等區域顯示正確的熱度數字。
   * 點擊載入「1995-1996 飛彈危機數據」，確認地圖上顯示「彭佳嶼海域」、「東山島海域」、「基隆/高雄外海」等區域的數據統計。
3. **互動連動測試**:
   * 將滑鼠滑過「西南空域」區塊，確認浮現 Tooltip。
   * 點擊「海峽中線」區塊，確認右側顯示中線的分析詳情。
   * 點擊「查看明細」按鈕，確認網頁能順利跳轉到「分類 (Categorize)」頁面，且表格僅列出包含中線的紀錄。
